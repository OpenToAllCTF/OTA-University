# Web Challenge Docker Template

This container is a template for PHP applications.

## Setup and deployment
- Put your source code in `./src`
- Run `docker-compose up`
- The challenge will be at `http://localhost:17000`
